 #include <stdio.h>

int main() {
    int pair,impair,N;
    printf("ecriver un nember :");
    scanf("%d",&N);
    if (N%2==0)
          printf ("un nombre   pair");
    else 
          printf("nombre  impair");

    return 0;
}